# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# production
/build
/.next

# vercel — link 后生成，含 orgId/projectId，且可能缓存凭据。本仓 PUBLIC，一律不入库
.vercel

# 浏览器自动化临时产物（截图/快照/console log），不属于源码
.playwright-mcp

# misc
.DS_Store
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
tsconfig.tsbuildinfo

# 客户材料 / 商业文档 — 本仓是 PUBLIC 仓库，Office 文档一律不入库。
# 起因：Union-Glen-网站策略方案.docx（自标「机密文件」，含竞品分析与转化策略）
# 曾被跟踪进公开仓库，2026-07-27 审查发现后 git rm --cached 移出。
# 客户材料放本地或客户资料库，不要放代码仓。
*.docx
*.doc
*.pptx
*.ppt
*.xlsx
*.xls
*.pdf

npm-debug.log*
yarn-debug.log*
yarn-error.log*


# playwright mcp
.playwright-mcp/

# claude code (local plans & config)
.claude/

# next.js auto-generated — rewritten between .next/types & .next/dev/types on every dev/build (avoid churn)
next-env.d.ts
